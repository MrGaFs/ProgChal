#include<stdlib.h>
#include<stdio.h>

int dialogues;
char lines[105][105];

int main() {
  // Read in
  scanf("%d\n", &dialogues);
  for (int i=0; i<dialogues; i++) {
    // Read in the dialogue, record length
    int length = 0;
    
  }
  return EXIT_SUCCESS;
}

void printResponse()
